import { CSSProperties } from 'react'

const styles: CSSProperties = {
    inset: 0,
    display: 'flex',
    position: 'fixed',
    alignItems: 'center',
    justifyContent: 'center'
}

export { styles }